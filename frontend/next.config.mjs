/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The floating dev indicator sits on top of the bottom tab bar; errors
  // are still surfaced without it.
  devIndicators: false,
};

export default nextConfig;
